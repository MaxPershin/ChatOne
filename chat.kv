#:import RiseInTransition kivy.uix.screenmanager.RiseInTransition
#:import C kivy.utils.get_color_from_hex

<TextInput>:
	multiline: False
	padding: [10, 0.3 * (self.height - self.line_height)]

<ChatLabel@Label>:
	color: C('#101010')
	text_size: (self.width, None)
	halign: 'left'
	valign: 'top'
	size_hint: (1, None)
	height: self.texture_size[1]

<ScrollView>:
	canvas.before:
		Color:
			rgb: 1, 1, 1
		Rectangle:
			pos: self.pos
			size: self.size

ScreenManager:
	transition: RiseInTransition()

	Screen:
		name: 'login'
		canvas:
			Color: 
				rgb: 1, 1, 1

			Rectangle:
				source: 'c.png'
				size: self.size
				pos: self.pos

		BoxLayout:
			padding: [5, 5]
			orientation: 'vertical'

			FloatLayout:

				Label:
					pos_hint: {"center_x": .2,"center_y": .55}
					size_hint: (1, .2)
					text: 'Nickname:'

				TextInput:
					id: nickname
					pos_hint: {"center_x": .5,"center_y": .55}
					size_hint: (.4, .05)
					text: ''
					hint_text: 'Your Name'


				Button:
					size_hint: (.3, .2)
					border: 0,0,0,0
					pos_hint: {'center_x': .54, 'center_y': .48}
					size_hint: (.9/3, .15/3)
					background_normal: "arrow.png"
					background_down: "arrow_p.png"
					on_press: app.pull_chat()
				


	Screen:
		id: chatroom
		name: 'chatroom'
		pos_hint: {"center_x": .5,"center_y": .5}

		BoxLayout:
			orientation: 'vertical'

			BoxLayout:
				size_hint: (1, .1)
				orientation: 'horizontal'

				Button:
					size_hint: (1, 1)
					font_name: 'mp.ttf'
					font_size: '50sp'
					background_normal: 'w_b.png'
					background_down: 'w_b.png'
					markup: True
					text: '[color=#000000]I[/color]'
					on_press: app.color(self)
					on_release: app.disconnect(self)
				Button:
					size_hint: (1, 1)
					font_name: 'mp.ttf'
					font_size: '50sp'
					background_normal: 'w_b.png'
					background_down: 'w_b.png'
					text: '[color=#000000]R[/color]'
					markup: True
					on_press: app.color2(self)
					on_release: app.color3(self)
					on_release: app.easy_refresh()
				Button:
					id: online_button
					font_name: 'mp.ttf'
					font_size: '50sp'
					markup: True
					background_normal: 'w_b.png'
					background_down: 'w_b.png'
					size_hint: (1, 1)
					text: '[color=#000000]w[/color]'
					on_press: app.online()

			ScrollView:
				ChatLabel:
					id: chat_logs
					text: ''
					font_size: '26sp'
					markup: True

			BoxLayout:
				height: 90
				orientation: 'horizontal'
				padding: 0
				size_hint: (1, None)

				TextInput:
					id: message
					multiline: True
					on_focus: app.focus(self)


				Button:
					font_name: 'mp.ttf'
					background_normal: 'send_b.png'
					font_size: '50sp'
					text: 'b'
					size_hint: (.35, 1)
					on_release: app.easy_send_msg()